- **Web 框架**：FastAPI (Python) 配合 `uvicorn` 运行环境。
- **下载引擎**：`yt-dlp` 用于解析和下载 3000+ 平台的媒体资源，辅以 `ffmpeg` 进行实时转码（AV1/HEVC 转 H.264）。
- **数据存储**：内置 `sqlite3` 存储用户凭证、任务队列及系统配置。
- **外部交互**：使用 `httpx` 异步请求 Telegram Bot API，利用 `psutil` 监控系统资源。