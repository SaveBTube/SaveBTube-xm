- 所有 API 请求统一通过 `src/utils/api.js` 中的 `request` 辅助函数发起，自动注入 `Authorization: Bearer <token>` 头。
- 认证状态（token, username）直接存储在 `localStorage` 中，并在路由守卫和 API 请求中同步读取。
- 视图组件采用 `<script setup>` 语法糖，结合 `vue-router` 的 `useRouter` 和 `useRoute` 进行导航控制。